$(document).ready(function(){
    fnSkill();
})

// 스킬 프로그레스바
function fnSkill(){
    var skillName = [];
    $('.skill-title').each(function(idx){
        var $this = $(this).text();
        skillName.push($this);

        var backColor;
        var pointColor;
    })
}